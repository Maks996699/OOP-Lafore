#include <iostream>
#include <iomanip>

using namespace std;

const int MAX = 20;

void reversit(char[MAX]);

int main()
{
	char str[MAX];
	cin.get(str, MAX);

	return 0;
}

void reversit(char arr[MAX])
{
	for (int i = 0; i < length; i++)
	{

	}
}